apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: allconnect
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Uses default StorageClass - works on Docker Desktop, Minikube, and most cloud providers
  # storageClassName: standard
